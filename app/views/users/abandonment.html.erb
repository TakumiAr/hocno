<% @user.posts.where(status: 2).each do |post| %>
  <%= link_to post do %>
    <div>
      <p><%= image_tag post.image.url %></p>
      <p>タイトル <%= post.title %></p>
      <p>カテゴリー <%= post.category.name %></p>
      <p><%= post.status %></p>
    </div>
  <% end %>
<% end %>

